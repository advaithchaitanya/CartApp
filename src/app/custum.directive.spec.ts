import { CustumDirective } from './custum.directive';

describe('CustumDirective', () => {
  it('should create an instance', () => {
    const directive = new CustumDirective();
    expect(directive).toBeTruthy();
  });
});
